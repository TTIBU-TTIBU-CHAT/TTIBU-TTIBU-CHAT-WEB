spring:
  datasource:
    url: jdbc:postgresql://db:5432/ttibu
    username: tibutibu
    password: tibutibu
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 10
      connection-timeout: 30000

server:
  port: 8080
